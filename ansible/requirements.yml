---
# Colecoes necessarias. Instale com:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: amazon.aws
    version: ">=8.0.0"
  - name: community.docker
    version: ">=3.10.0"
